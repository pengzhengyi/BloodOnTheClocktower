import type { IRequest } from '../../../common/interfaces/request';

/**
 * Elevation request for a user. It will enable more features like viewing debugging logs.
 */
export interface IElevateRequest extends IRequest {}
