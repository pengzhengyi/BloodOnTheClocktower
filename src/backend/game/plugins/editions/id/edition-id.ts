/**
 * Unique global identifier for an edition.
 *
 * For official editions, the id is derived from its name.
 * For user-defined editions, the id is a string containing custom name and unique identifier
 */
export type EditionId = string;
