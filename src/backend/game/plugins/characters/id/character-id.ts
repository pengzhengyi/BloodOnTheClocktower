/**
 * Unique global identifier for a character.
 *
 * For official characters, the id is derived from its name.
 * For user-defined characters, the id is a string containing custom name and unique identifier
 */
export type CharacterId = string;
