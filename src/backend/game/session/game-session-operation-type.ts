/**
 * The type of operation to perform on a game session manager.
 */
export enum GameSessionOperationType {
    Find = 'find',
    Create = 'create',
    End = 'end',

    CreateGame = 'create game',
}
